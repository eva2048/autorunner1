//加入项目
define(['app'], function(app) {
    app.register
        .controller('tipmodalCtrl', ['$rootScope', '$scope', '$modal', '$log',
            function($rootScope, $scope, $modal, $log) {
                $scope.createTip = function(tiptype,tipcontent) {
                    
                };
            }
        ])
})
